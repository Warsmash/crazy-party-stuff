<%= simple_form_for(attraction) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.input :one_liner %>
  <%= f.input :description %>
  <%= f.submit "Save attraction", class: "btn btn-primary" %>
<% end %>
